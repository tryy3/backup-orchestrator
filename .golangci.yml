# golangci-lint configuration
# https://golangci-lint.run/usage/configuration/
version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck       # check for unchecked errors
    - govet          # go vet equivalent
    - ineffassign    # detect ineffectual assignments
    - staticcheck    # staticcheck suite (includes gosimple)
    - unused         # check for unused code
    - gocritic       # opinionated checks
    - misspell       # catch common spelling mistakes
    - noctx          # http requests without context
    - bodyclose      # HTTP response body close check
    - prealloc       # slice preallocation hints

  settings:
    errcheck:
      check-type-assertions: true
    govet:
      enable-all: true
      disable:
        - fieldalignment # run `betteralign -fix ./...` for any remaining struct optimizations
    gocritic:
      enabled-tags:
        - diagnostic
        - style
        - performance
      disabled-checks:
        - hugeParam      # too noisy for this project size
        - rangeValCopy   # too noisy for this project size

  exclusions:
    rules:
      # Test files get a relaxed ruleset
      - path: _test\.go
        linters:
          - errcheck
          - gocritic

formatters:
  enable:
    - gofmt

issues:
  max-same-issues: 0
